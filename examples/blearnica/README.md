# Blearnica Example

This example shows how a project can use `agent-harness` to move from idea to
MVP planning.

It is not a full application. It is filled project memory that demonstrates how
to use the harness files.
