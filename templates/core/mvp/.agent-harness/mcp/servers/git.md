# Git MCP Notes

Use Git tools to inspect repository history, diffs, and status when available.

Default behavior:

- Read status before summarizing local changes.
- Never discard user changes without explicit approval.
- Keep commits focused when the user asks for commits.
